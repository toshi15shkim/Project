<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tems.opr.equipGroup.dao.TrafficMapper">

    <!-- 교통 설비 조회 -->
    <select id="selectTrafficList" parameterType="hashmap" resultType="map">
        SELECT TUNNEL_ID
            , TUNNEL_NAME
            , TUNNEL_LINE
            , TUNNEL_LENGTH
            , TUNNEL_UP
            , TUNNEL_DOWN
            , TUNNEL_INFO
            , TUNNEL_USE
            , IP
            , PORT
            , STATION
            , POS_START
            , POS_END
        FROM TUNNEL
        WHERE TUNNEL_USE = 'Y'
        <if test="tunnel_id != null">
            AND TUNNEL_ID = #{tunnel_id}
        </if>
        ORDER BY TUNNEL_ID
    </select>


</mapper>

